using UnityEngine;

public class GameStateMachine : MonoBehaviour
{

    //Initiazers 

    //Get player(s)

    //Score




    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    void OnStateEnter(GameStateMachine gameState)
    {
        
    }

    void OnStateChange(GameStateMachine gameState)
    {

    }

    void OnStateExit(GameStateMachine gameState)
    {

    }


    // Update is called once per frame
    void Update()
    {
        
    }
}
